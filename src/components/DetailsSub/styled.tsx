import styled from "styled-components";

export const GroupInfo = styled.div`
  margin-bottom: 30px;
`;

export const TitleSubStyled = styled.h2`
  font-size: 22px;
  font-weight: 600;
  color: var(--color-violet-dark);
  font-family: var(--font-default);
`;

export const TextResult = styled.p`
  font-size: 18px;
  font-family: var(--font-default);
  margin: 8px 0 0 8px;
`;
